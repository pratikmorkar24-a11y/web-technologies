SERVER_PORT=8080

# ---- MySQL Database ----
DB_HOST=localhost
DB_PORT=3306
DB_NAME=inkwell_bookstore
DB_USER=bookstore_user
DB_PASSWORD=bookstore_pass

# ---- CORS (allowed origin for the React dev server) ----
CORS_ORIGIN=http://localhost:3000

# ==========================================================================
# Frontend (React) — create frontend/.env if you need to override the API URL
# ==========================================================================
# REACT_APP_API_URL=http://localhost:8080/api
